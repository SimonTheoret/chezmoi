gui: 
  nerdFontsVersion: "3"
  theme:
    lightTheme: false


os:
  editPreset: 'nvim'

git:
  paging:
    # colorArg: always
    # pager: delta --syntax-theme="Monokai Extended" --paging=never --line-numbers --hyperlinks --hyperlinks-file-link-format="lazygit-edit://{path}:{line}"

customCommands:
  - key: "C"
    command: "git cz c"
    description: "commit with commitizen"
    context: "files"
    loadingText: "opening commitizen commit tool"
    output: terminal
